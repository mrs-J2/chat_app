name: chat_app_main
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.0
  cupertino_icons: ^1.0.8
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  image_picker: ^1.1.2  
  file_picker: ^10.3.3
  firebase_storage: ^13.0.3
  cached_network_image: ^3.4.1
  video_player: ^2.10.0
  url_launcher: ^6.3.2
  chewie: ^1.0.8
  flutter_chat_bubble: ^2.0.2
  flutter_chat_ui: ^1.6.9 # Use latest stable version
  flutter_chat_types: ^3.6.1
  intl: ^0.18.1
  provider: ^6.0.5
  flutter_image_compress: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  